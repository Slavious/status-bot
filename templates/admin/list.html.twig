{% extends 'admin/base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col">
            <table class="table">
                <thead>
                    <th scope="col">#</th>
                    <th scope="col">name</th>
                    <th scope="col">domain</th>
                    <th scope="col">priority</th>
                    <th scope="col"></th>
                </thead>
                <tbody>
                    {% for site in sites %}
                        <tr>
                            <td>{{ site.id }}</td>
                            <td>{{ site.name }}</td>
                            <td>{{ site.domain }}</td>
                            <td>{{ site.priority }}</td>
                            <td>
                                <a class="btn btn-primary" href="{{ path('site_edit', { siteId: site.id }) }}">
                                    Edit
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}